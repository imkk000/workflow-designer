import nodeContextMenu from './nodeContextMenu'
import drawAreaContextMenu from './drawAreaContextMenu'

// NOTE: load all context menu
$(document).ready(() => {
  nodeContextMenu()
  drawAreaContextMenu()
})
